# Updates

## 2026-08-27
- **kenmacpherson.com**: Added a deployment note about host renames leaving orphaned nginx configs behind. When a host changes names, remove the old config from `sites-enabled/` and either delete it or move it to `sites-available/deprecated/` so it cannot keep serving the old root.

## 2026-01-22
- **wealthstrategyguy.com**: Fixed color contrast for hero section byline "Making the Difficult, Simple." to be white on dark blue.
