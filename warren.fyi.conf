# 
#   setup webshoting other webstie.com
#  /etc/nginx/conf.d/*.conf
# ln -s /etc/nginx/sites-available/quentinworld.conf /etc/nginx/sites-enabled/quentinworld.conf
# systemctl restart nginx

server {
    listen 80;
    listen [::]:80;

    server_name  warren.fyi www.warren.fyi;
    root /var/www/warren.fyi;
    index index.html index.htm;
    access_log /var/log/nginx/warren.fyi.access.log main;

    log_format main '$http_x_forwarded_for - $remote_user [$time_local] ' '"$request_method $scheme://$host$request_uri $server_protocol" ' '$status $body_bytes_sent "$http_referer" ' '"$http_user_agent" $request_time';

   location / {
       try_files $uri $uri/ =404;
   }

}